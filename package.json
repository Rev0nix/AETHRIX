{
  "name": "aethrix",
  "version": "1.0.0",
  "private": true,
  "description": "AETHRIX — premium multi-category e-commerce platform (Electronics, Fashion, Smart Gadgets, Home Decor, Fitness, Accessories)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "seed": "npm run seed --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "concurrently -n API,WEB,ADMIN -c blue,green,magenta \"npm:dev:backend\" \"npm:dev:frontend\" \"npm:dev:admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
